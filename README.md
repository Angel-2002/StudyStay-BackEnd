# StudyStay-BackEnd
Python (FastAPI) and SQL

# Crear entorno dentro de la carpeta
py -m venv env
# Activar entorno
env/Scripts/activate
# Instalar
 pip install fastapi
 pip install uvicorn
 pip install sqlalchemy
 pip install pymysql